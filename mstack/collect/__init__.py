"""수집 엔진 -- 리더암·로봇·카메라를 실제로 돌려 에피소드를 만드는 곳.

Qt 시그널을 내보내지만 위젯이 아니다. 로봇(mstack.robots), 텔레옵
플러그인(mstack.agents), 노드 통신(mstack.comm)을 직접 알아야 하므로
mstack/gui 에 있으면 "상위 계층은 구체 하드웨어를 모른다"는 규칙이 깨진다
(2026-09-02 에 mstack/gui 에서 옮겨 왔다).
"""
