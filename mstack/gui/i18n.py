"""GUI 문자열 정책 — 언어 전환은 없다 (2026-09-05 결정).

한↔영 토글과 211줄짜리 대역표가 있었지만 없앴다. 두 가지 이유다:

* **반쪽만 동작했다.** ``tr()`` 은 위젯을 만들 때 한 번 불리고 그 문자열이
  박히므로, 전역 언어를 바꿔도 이미 떠 있는 창은 그대로였다 -- 전환 뒤 새로
  여는 다이얼로그만 바뀌어 한국어와 영어가 섞였다. 제대로 하려면 모든
  위젯에 retranslate 경로가 필요한데, 그 설계 비용이 얻는 것보다 컸다.
* **아무도 찾지 않았다.** 설정 깊숙이 숨은 언어 전환은 아이폰을 여럿이
  써도 아무도 언어를 바꾸지 않는 것과 같다. 실제로 그 버튼은 비활성인 채
  "미개발" 표시를 달고 있었다.

대신 **문자열마다 언어를 고정**한다. 외국인 연구원과 한국인이 같은 기계
앞에서 번갈아 수집하므로, 언어를 바꾸는 것이 아니라 **무엇을 어느 언어로
쓸지**를 계층으로 나눈다. 기준은 "0.5초만 보고 눌러야 하는가, 읽고
이해해야 하는가"다 -- 수집은 손이 빠르고 실수가 곧 데이터 오염이나 충돌로
이어진다.

    Action    누르는 것          영어   Connect / Save / Discard / Home
    Status    한눈에 읽는 상태    영어   RECORDING / IDLE / GATE
    Identity  식별자             영어   cam1 / agent / S015 / I003
    Guide     안내·툴팁·경고      한국어  "물체를 제자리에 놓은 뒤 누르세요"
    Log       로그·예외           한국어  "[카메라노드] 스트림 시작 ..."

앞의 셋은 읽는 글이 아니라 **기호로 인식되는 것**이라 영어가 오히려 빠르고,
뒤의 둘은 정확히 이해해야 하므로 대부분의 사용자인 한국인의 모국어로 둔다.

``tr()`` 은 호출부를 그대로 두기 위해 항등 함수로 남긴다. 수백 곳의
``tr("...")`` 을 걷어내는 diff 는 검토가 불가능하고, 문자열을 감싸 두면
나중에 정말 필요해졌을 때 여기 한 곳만 고치면 된다.
"""

from __future__ import annotations


def tr(text: str) -> str:
    """문자열을 그대로 돌려준다 (위 정책 참고).

    남겨 둔 이유는 호출부 보존이다 -- 지우려면 수백 곳을 건드려야 하고,
    그 diff 에서 진짜 문자열 변경을 가려내기 어렵다.
    """
    return text
